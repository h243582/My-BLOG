server:
  port: 8091 #端口号
#eureka:
#  client:
#    service-url:
#      defaultZone: http://127.0.0.1:6868/eureka/ #eureka客户端和服务器端交互的地址,6868是eureka服务的地址
#  instance:
#    prefer-ip-address: true  #能否跨服务器访问（就是说访问另一个服务）
spring:
  application:
    name: ly-item(物品)
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mydb?characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: 123456
  jpa:
    database: mysql
    generate-ddl: true
    show-sql: true
